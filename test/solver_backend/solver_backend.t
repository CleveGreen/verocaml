The direct bridge parity, capability preflight, cleanup/isolation, and pinned
backend profile remain an explicit architecture specialist.  Its transcript is
not an ordinary formatting or ordering contract.

  $ timeout 120s ./solver_backend_tool.exe unit >/dev/null

The real one-millisecond timeout smoke is separately bounded and specialist.
It proves only that the pinned backend remains callable and classified.

  $ timeout 15s ./solver_backend_tool.exe timeout-smoke >/dev/null
